type Cliente{
    id:ID
    nome:String
    email:String
}

extend type Query {

    cliente(id:ID):Cliente

    clientes:[Cliente]
}

extend type Mutation {
    save(id:ID, nome:String, email:String):Cliente
    delete(id:ID):Boolean
}